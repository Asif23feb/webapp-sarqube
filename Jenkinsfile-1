pipeline {
  agent any
  
  stages {
    stage('Scan') {
      steps {
        withSonarQubeEnv(installationName: 'sonarqube7.1') { 
          sh './mvnw clean install sonarqube:sonarqube'
        }
      }
    }
  }
}
